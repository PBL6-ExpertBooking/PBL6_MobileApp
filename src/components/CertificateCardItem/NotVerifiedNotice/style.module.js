import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  modalContainer: {
    width: '95%',
    backgroundColor: 'white',
    alignSelf: 'center',
    padding: 20,
    maxHeight: '50%',
    borderRadius: 10,
  },
})
