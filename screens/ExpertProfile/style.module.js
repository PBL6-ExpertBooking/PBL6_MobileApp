import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 0,
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  profileInfo: {
    width: '100%',
    alignItems: 'center',
  },
  descriptions: {
    width: '80%',
    marginTop: 10,
    padding: 5,
    borderColor: '#000',
    borderWidth: 1,
    borderRadius: 5,
  },
})
