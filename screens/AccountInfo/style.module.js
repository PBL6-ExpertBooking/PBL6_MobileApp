import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 0,
    backgroundColor: '#fff',
    alignItems: 'center',
  },
  avatarContainer: {
    marginBottom: 20,
    alignItems: 'center',
    gap: 5,
  },
  expertRegister: {
    flex: 0,
    flexDirection: 'row',
    position: 'absolute',
    bottom: 10,
    gap: 10,
  },
})
