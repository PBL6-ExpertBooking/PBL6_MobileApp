import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 0,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  detailNavigator: {
    flexDirection: 'row',
  },
  textStyle: {
    color: 'black',
    fontSize: 16,
    fontWeight: 700,
  },
})
