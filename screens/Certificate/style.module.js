import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
  },
  certListView: {
    gap: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  certListStyle: {
    width: '90%',
  },
})
